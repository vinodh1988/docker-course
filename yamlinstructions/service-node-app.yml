apiVersion: v1
kind: Service
metadata:
  name: service-node-app
  labels:
    app: node-app
spec:
  type: LoadBalancer
  selector:
    app: node-app
    deployment: node-deployment
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
